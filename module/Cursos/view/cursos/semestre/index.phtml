<h4 class="d-flex justify-content-between align-items-center mb-3 mt-3">
   <span class="text-muted">Todos los semestres</span>
   <a class="btn btn-sm btn-info" href="<?= $this->url('semestres', ['action' => 'create'])?>">Crear</a>
</h4>
<table class="table table-hover">
   <thead>
   <tr>
       <th>Id</th>
       <th>Fecha fin</th>
       <th>Fecha inicio</th>
       <th>Acciones</th>
   </tr>
   </thead>
   <?php foreach ($this->semestres as $semestre) : ?>

       <tr>
           <td><?= $this->escapeHtml($semestre->id) ?></td>
           <td><?= $this->escapeHtml($semestre->fecha_inicio) ?></td>
           <td><?= $this->escapeHtml($semestre->fecha_fin) ?></td>
           <td>
                <a class="btn btn-info btn-sm p-1" href="<?= $this->url('semestres', [
                    'action' => 'edit',
                    'id' => $semestre->id
                    ]) ?>"
                >
                    Editar
                </a>
           <td>
                <a class="btn btn-danger btn-sm p-1" href="<?= $this->url('semestres', [
                    'action' => 'delete',
                    'id' => $semestre->id
                    ]) ?>"
                    onclick="return confirm('Seguro que quiere eliminar?')"
                >
                    Eliminar
                </a>
            </td>
       </tr>
   <?php endforeach; ?>
</table>
