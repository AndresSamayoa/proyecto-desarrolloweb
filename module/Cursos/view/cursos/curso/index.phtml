<h4 class="d-flex justify-content-between align-items-center mb-3 mt-3">
   <span class="text-muted">Todos los cursos</span>
   <a class="btn btn-sm btn-info" href="<?= $this->url('cursos', ['action' => 'create'])?>">Crear</a>
</h4>
<table class="table table-hover">
   <thead>
   <tr>
       <th>Id</th>
       <th>Nombres</th>
       <th>Acciones</th>
   </tr>
   </thead>
   <?php foreach ($this->cursos as $curso) : ?>

       <tr>
           <td><?= $this->escapeHtml($curso->id) ?></td>
           <td><?= $this->escapeHtml($curso->nombre) ?></td>
           <td>
                <a class="btn btn-info btn-sm p-1" href="<?= $this->url('cursos', [
                    'action' => 'edit',
                    'id' => $curso->id
                    ]) ?>"
                >
                    Editar
                </a>
                <a class="btn btn-danger btn-sm p-1" href="<?= $this->url('cursos', [
                    'action' => 'delete',
                    'id' => $curso->id
                    ]) ?>"
                    onclick="return confirm('Seguro que quiere eliminar?')"
                >
                    Eliminar
                </a>
            </td>
       </tr>
   <?php endforeach; ?>
</table>
